// Normalerweise leer oder nur Plugin-Management, wenn benötigt.
// In den meisten Projekten liegt die Konfiguration in `settings.gradle.kts`.
// Beispiel:
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Falls du zusätzliche Build-Tools brauchst
    }
}
